This is the kumiai discord bot

To run this project do the following:
1) Run this in the root directory to install all dependencies: npm install
2) Run this in the root directory to start up the bot: node index.js

** Make sure you have the mongoURI, your server id and a bot token in the .env file prior to running the project.
** Make sure your bot has the required permissions to perform the actions outlined.
