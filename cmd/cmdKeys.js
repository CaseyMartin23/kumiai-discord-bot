const PREFIX = "!";

const CMDNAMES = {
  RANK_CMD: "rank",
};

const CMDAGRS = {
  ADD: "-a",
  REMOVE: "-r",
  RANK_NAME: "r:",
  RANK_ID: "i:",
  RANK_POINTS: "p:",
};

module.exports = { PREFIX, CMDAGRS, CMDNAMES };
